# Dayflow Frontend

This directory is owned by **Person 3 (Frontend)**.

The backend API lives in `../backend/`. See [../docs/API_CONTRACT.md](../docs/API_CONTRACT.md) for endpoint contracts.

Base API URL (local): `http://localhost:8000/api/v1`
