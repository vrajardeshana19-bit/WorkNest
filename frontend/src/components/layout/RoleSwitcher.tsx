import React from 'react';

export const RoleSwitcher: React.FC = () => {
  // Minimal clean production shell — role switching is accessible via user profile dropdown & login presets
  return null;
};
